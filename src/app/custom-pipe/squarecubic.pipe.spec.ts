import { SquarecubicPipe } from './squarecubic.pipe';

describe('SquarecubicPipe', () => {
  it('create an instance', () => {
    const pipe = new SquarecubicPipe();
    expect(pipe).toBeTruthy();
  });
});
